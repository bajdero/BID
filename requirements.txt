# BID — Python dependencies
# Install with: pip install -r requirements.txt

# Production
Pillow>=10.0.0
tzdata>=2024.1        # IANA timezone database for ZoneInfo on Windows

# Testing
pytest>=7.0
pytest-html>=4.0
